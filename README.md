# Athena
A neural network architecture for sequence modeling that is cheaper than Transformer in hardware (less memory and less computation, especially in inference) without losing the ability to memorize and retrieve accurate context information.
